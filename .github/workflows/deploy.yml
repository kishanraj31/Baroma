name: Deploy Website to GCP VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Deploy to server via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: 136.115.159.7
        username: kishanraj8601
        key: ${{ secrets.VM_SSH_KEY }}
        script: |
          cd ~
          ./deploy.sh